# Baseline de deute CSRF backend admin preexistent (#1030).
# Format exacte: app/api/admin/.../route.ts -> METHOD() sense verifyCsrf
# Qualsevol entrada nova falla el guard; qualsevol entrada arreglada queda stale i s'ha d'esborrar.
